# 이 파일을 .env로 복사하고 값을 채우면 npm run dev 실행 시 자동으로 적용됩니다.
# 둘 다 선택 사항이며, 비워두면 기존 기본값(5173 / localhost:8080)이 사용됩니다.

# vite dev 서버가 뜰 포트
# VITE_DEV_PORT=5173

# /api 요청을 프록시할 Go 서버 주소 (DBTOOL_LISTEN_ADDR을 바꿨다면 여기도 맞춰주세요)
# VITE_API_PROXY_TARGET=http://localhost:8080
